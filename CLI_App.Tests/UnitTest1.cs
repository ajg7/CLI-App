using Spreetail_Take_Home.Core;

namespace Spreetail_Take_Home.Tests
{
    public class AddTests
    {

       /* [Fact]
        public void Add_WithNewKey_AddsKey()
        {
            MultiValueDictionary multiValueDictionary = new MultiValueDictionary();
            string key = "Batman";
            string value = "Penguin";
            multiValueDictionary.Add(key, value);

            List<string> members = multiValueDictionary.GetMembers(key);
            Assert.Contains(value, members);
        }

        [Fact]
        public void Add_WithExistingKey_AddsValue()
        {
            MultiValueDictionary dictionary = new MultiValueDictionary();
            string key = "superman";
            string value1 = "LexLuthor";
            string value2 = "ToyMan";
            dictionary.Add(key, value1);

            dictionary.Add(key, value2);

            List<string> members = dictionary.GetMembers(key);
            Assert.Equal(2, members.Count);
            Assert.Contains(value1, members);
            Assert.Contains(value2, members);
        }*/

    }
}
